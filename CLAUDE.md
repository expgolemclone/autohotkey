## rules

1. 文字コードはutf-8, LF.
2. hooksはpython.
3. 短いホットストリングは長いもののプレフィックスになり競合する (例: `//r` は `//res` `//ref` `//rep` と競合).

## after edits

1. 動作確認をする.
2. ユーザに許可をもらい, commit and push . pushは許可不要. (スコープ外の未コミット変更は質問ポップアップを出して, 分割してcomitするか決める)
